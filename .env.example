KV_REST_API_READ_ONLY_TOKEN=
KV_REST_API_TOKEN=
KV_REST_API_URL=
KV_URL=
REDIS_URL=

# GA4 measurement ID, e.g. G-XXXXXXXXXX. Leave empty to skip loading gtag.js —
# do that in dev and on previews so they don't land in production's numbers.
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=
